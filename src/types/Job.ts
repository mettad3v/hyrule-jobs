// prettier-ignore
interface Job {
    id: number;
    title: string;
    location: string;
    number: number;
    salary: number;
}

export default Job;


//how to loop through data in Laravel Blade??